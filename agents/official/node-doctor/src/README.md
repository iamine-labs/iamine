# Compiled Program

The executable implementation is compiled into `iamine-node` as the official
Rust program in `iamine-node/src/node_doctor_agent/`. Package bytes are never
interpreted as executable code.
