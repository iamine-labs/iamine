# Resource Review

The local-readonly profile is bounded to one background thread, 512 MiB
working set, no cache, no dynamic probe, no accelerator, no network, no model,
no worker, no scheduler override, and no service or VM mutation.
