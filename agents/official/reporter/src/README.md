# Reporter Runtime Binding

The reviewed implementation is compiled in
`iamine-node/src/reporter_agent/`. This package directory contains no script,
binary, shell entrypoint, model, network adapter, file collector, or exporter.
