[package]
name = "iamine-hardware"
version = "0.1.0"
edition = "2021"

[dependencies]
dirs = "5.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
tempfile = "3"
