# v0.8 LAN Beta Closeout

Feature:

```text
LAN-INFERENCE-BETA-001
```

## Scope

This document records the Milestone 1 / v0.8 release-gate evidence for the
installable LAN beta. It does not add new runtime behavior. It closes only when
the milestone QA matrix proves the beta contract across local, TS140, and
Proxmox/R5500 environments.

## Covered Product Features

Milestone 1 is composed of these closed feature gates in `develop`:

| Feature | Evidence | Release-gate responsibility |
| --- | --- | --- |
| LAN-INFERENCE-BETA-CONTRACT-001 | `b8d455b` | Supported topology, flows, failure semantics, and release boundaries. |
| LAN-NODE-DOCTOR-001 | `091c89b` | Diagnostics before runtime without hidden inference startup. |
| LAN-WORKER-LIFECYCLE-001 | `53eed90` | Explicit install, start, stop, restart, readiness, and recovery behavior. |
| NODE-CONFIG-SCHEMA-MIGRATION-001 | `927495a` | Versioned configuration migration and rollback. |
| WORKER-METRICS-PORT-ALLOCATION-001 | `66c43b9` | Deterministic, non-conflicting worker metrics endpoints. |
| LAN-INFERENCE-CLI-001 | `d1f5c90` | Supported LAN inference entrypoint with clear errors. |
| LAN-REAL-INFERENCE-RESILIENCE-001 | `e923a59` | Bounded retry and failure recovery during LAN inference. |
| V1-OBSERVABILITY-001 (LAN phase) | `8ca91d9` | Privacy-safe correlated setup, dispatch, execution, recovery, and result evidence. |
| LAN-INFERENCE-BETA-PACKAGING-001 | `d352381` / merge `27bc8df` | Clean install, upgrade, service templates, package manifest, and rollback artifacts. |

## Release Gate

The beta release gate must prove:

- clean install or upgrade path;
- one to five workers;
- real inference over LAN;
- diagnostics before runtime;
- reboot recovery;
- worker stop, restart, and readiness recovery;
- bounded failure recovery;
- rollback path;
- privacy-safe support evidence.

## Milestone QA Matrix

| Check | Mac | TS140 | Proxmox/R5500 |
| --- | --- | --- | --- |
| Git identity and exact scope | PASS | PASS | PASS |
| Packaging smoke and manifest privacy | PASS | PASS | PASS |
| Diagnostics before runtime | PASS WITH BASELINE WARNING | PASS WITH BASELINE WARNING | PASS WITH BASELINE WARNING |
| Config migration and rollback smoke | PASS | PASS | PASS |
| Worker lifecycle readiness | PASS | PASS | PASS |
| Cluster status human and JSON | PASS | PASS | PASS |
| LAN inference success | PASS AS CLIENT | FIELD BLOCKED AFTER WORKER READY | PASS |
| Worker restart recovery | N/A | N/A | PASS |
| Bounded failure/retry evidence | UNIT AND FIELD BASELINE | FIELD BLOCKED AFTER WORKER READY | PASS |
| Upgrade and rollback path | LOCAL SMOKE PASS | LOCAL SMOKE PASS | LOCAL SMOKE PASS |
| Quality gate | PASS WITH WARNINGS | N/A | N/A |

## Validation Commands

The closeout pass uses the canonical commands plus feature-specific smokes:

```bash
./scripts/quality-gate.sh
scripts/lan-beta-package.sh --no-build --binary target/debug/iamine-node --output-dir <isolated-output>
./target/debug/iamine-node --help
./target/debug/iamine-node lan doctor --json
./target/debug/iamine-node node config status --json
./target/debug/iamine-node worker lifecycle status --json
./target/debug/iamine-node worker lifecycle readiness --json
./target/debug/iamine-node cluster status
./target/debug/iamine-node cluster status --json
```

Field runs may add worker start, stop, restart, real LAN inference, failure
injection, and rollback checks when the environment is available and authorized.

## Mac Local Evidence

The Mac local pass validated the release-gate surfaces without starting workers
or running inference:

- branch `feature/lan-inference-beta-001` from base
  `27bc8df3365294a0883c5319b765e4a1ffd53981`;
- roadmap and closeout changes only; no Rust runtime files changed;
- `main.rs` remains 4872 lines and `cluster_registry.rs` remains 876 lines;
- `iamine-node --help` exposes LAN doctor, LAN infer, node config, worker
  lifecycle, and cluster status commands;
- `iamine-node lan doctor --json` returned schema `1.0.0`, preserved runtime
  side effects as false, and reported the local node config as legacy/migrable;
- isolated `node config migrate`, `status`, and `rollback` smokes passed in
  `/tmp`;
- `worker lifecycle status --json` and `readiness --json` reported explicit
  manual service-manager behavior and no runtime start/stop side effects;
- `cluster status` and `cluster status --json` completed with no LAN nodes
  discovered in the local-only environment;
- `scripts/lan-beta-package.sh --no-build` produced a package and archive in
  an isolated `/tmp` directory;
- manifest JSON parsed successfully, kept artifact paths relative, recorded no
  forbidden local paths or host identifiers, and all runtime effects were false;
- `./scripts/quality-gate.sh` returned `PASS WITH WARNINGS` with
  `required_failures=0`, `warnings=0`, and `skipped=3`.

Accepted local warnings:

- the default Mac node config is legacy and intentionally reported as
  migrable; isolated migration and rollback smokes passed;
- optional tools `cargo audit`, `cargo deny`, and `gitleaks` were unavailable
  and were reported as skipped;
- existing Rust and clippy warnings remain historical and were not introduced
  by this documentation-only release gate.

## TS140 Evidence

The TS140 pass validated the same non-destructive release-gate surfaces after
synchronizing the working copy to the exact feature identity:

- path `/home/ts140/iamine`;
- branch `feature/lan-inference-beta-001`;
- HEAD `7c108e7d427df01a93fdca66c4d059b2b98f9b58`;
- tree `2d92b6cfcbef86f2c2b89416b099eb561ba4e41c`;
- base `27bc8df3365294a0883c5319b765e4a1ffd53981`;
- tracked and staged state clean before and after QA;
- preserved 34 untracked QA artifacts with pre-sync hash
  `4ba39204db4a02bcb033196a8ce00affda82a9a9b664b654fb3f1ff870196bab`;
- non-interactive SSH shell did not include `cargo` in `PATH`; QA reran with
  `/home/ts140/.cargo/bin` explicitly prepended;
- `cargo build -p iamine-node` passed with historical warnings;
- package assembly and manifest privacy validation passed in isolated `/tmp`;
- `iamine-node --help` exposed the expected LAN beta commands;
- `iamine-node lan doctor --json` returned `warn`, preserved all runtime side
  effects as false, and reported the local config baseline as legacy/migrable;
- isolated `node config migrate` and `rollback` smokes passed in `/tmp`;
- `worker lifecycle status --port=7002 --json` and
  `readiness --port=7002 --json` returned `manual` with runtime effects false;
- `cluster status` and `cluster status --json` completed with cluster
  `default-lan` and zero discovered nodes in the non-runtime TS140 check;
- `git diff --check` and `git diff --cached --check` passed.
- after TS140 evidence was committed, TS140 was fast-forwarded from
  `9cc91c03f9b597a0735f6b8401238e0bfdda9a5f` to the current HEAD
  `7c108e7d427df01a93fdca66c4d059b2b98f9b58`;
- package manifest and diff checks were repeated on the current HEAD and
  passed.

Accepted TS140 warnings:

- the default TS140 node config is legacy and intentionally reported as
  migrable; isolated migration and rollback smokes passed;
- `cargo` is available under `/home/ts140/.cargo/bin` but not in the default
  non-interactive SSH `PATH`;
- existing Rust warnings remain historical and were not introduced by this
  documentation-only release gate.

TS140 field note:

- a temporary TS140 worker in mock/skip mode started on port `7002`, emitted
  worker startup and PubSub readiness evidence, and was stopped cleanly;
- the Mac controller-side broadcast was blocked before network startup by a
  local `Operation not permitted` error while opening the P2P runtime;
- this is recorded as a field environment/harness blocker for the TS140
  controller path, not as a product runtime regression.

## Proxmox/R5500 Evidence

The Proxmox/R5500 pass covered four guests:

```text
iamine-ctrl
iamine-wrk1
iamine-wrk2
iamine-heavy
```

Before synchronization, all four guests were on
`feature/lan-inference-beta-packaging-001` at
`d35238177141685b45d80ee645e4e750d924db20`, with tracked and staged state
clean. Each preserved the same two untracked QA log artifacts with hash
`5277a97d957b70e7cba217f4abcb8002ee2c570817957818781c60dd8446295a`.

Each guest was synchronized with:

```bash
git fetch origin +refs/heads/feature/lan-inference-beta-001:refs/remotes/origin/feature/lan-inference-beta-001
git switch feature/lan-inference-beta-001
git merge --ff-only origin/feature/lan-inference-beta-001
```

After synchronization, all four guests validated:

- branch `feature/lan-inference-beta-001`;
- HEAD `7c108e7d427df01a93fdca66c4d059b2b98f9b58`;
- tree `2d92b6cfcbef86f2c2b89416b099eb561ba4e41c`;
- base `27bc8df3365294a0883c5319b765e4a1ffd53981`;
- tracked and staged state clean.

The non-destructive Proxmox pass ran on all four guests:

- `cargo build -p iamine-node`;
- isolated package assembly and manifest privacy validation;
- `iamine-node --help`;
- `iamine-node lan doctor --json`;
- `iamine-node node config status --json`;
- isolated `node config migrate` and `rollback` smokes in `/tmp`;
- `worker lifecycle status --json`;
- `worker lifecycle readiness --json`;
- `cluster status` and `cluster status --json`;
- `git diff --check` and `git diff --cached --check`.

All four guests passed the non-destructive pass. Rust warnings were historical.

The Proxmox field pass then validated temporary QA-owned runtime behavior:

- workers started in safe mock/skip mode on `iamine-wrk1:4101`,
  `iamine-wrk2:4102`, and `iamine-heavy:4103`;
- `iamine-ctrl` cluster status observed three LAN nodes;
- broadcast `reverse_string` from `iamine-ctrl` returned the expected reversed
  output;
- controller logs included task offer publication, bid receipt, assignment,
  result receipt, and final outcome markers;
- only QA-owned worker PIDs were stopped;
- `iamine-wrk1` was restarted on port `4101`;
- post-restart broadcast from `iamine-ctrl` passed with the expected result and
  final outcome markers;
- after all QA-owned workers were stopped, a no-workers broadcast exited with
  `pubsub_readiness_timeout` and no final success, proving the no-worker field
  path remained bounded instead of hanging indefinitely.

Real LAN inference was validated with the legacy CPU daemon E2E harness:

```bash
scripts/qa-legacy-cpu-daemon-e2e.sh
```

The harness ran a Mac client through `iamine-heavy` to the dedicated legacy CPU
daemon using `tinyllama-1b` and `max_tokens=8`. The first preflight identified
that the dedicated daemon binary was not present in the current checkout; the
QA run built it under `target/legacy-cpu` using
`scripts/build-legacy-cpu-daemon.sh`, then reran preflight successfully.

Final E2E evidence:

- remote host `iamine-heavy`;
- remote HEAD `7c108e7d427df01a93fdca66c4d059b2b98f9b58`;
- model `tinyllama-1b`;
- client status `0`;
- `result_received` present;
- `final_outcome_success` present;
- worker `worker_startup_ready`, `task_message_received`, `task_completed`,
  and `result_published` present;
- daemon execution marker present;
- SIGILL / illegal instruction absent;
- QA-owned worker and daemon cleanup passed;
- worker port `4103` was free after cleanup.

## Closeout Decision

Milestone 1 / v0.8 is closed by `LAN-INFERENCE-BETA-001` after exhaustive
milestone QA, controlled merge to `develop`, post-merge validation, and roadmap
state update to `CLOSED`.
